# 领取奖励

领取奖励调用 MAA 的 `Award` 任务。可分别启用：

- 每日与每周任务奖励；
- 邮件；
- 免费单抽；
- 合成玉；
- 限时采矿或活动奖励。

建议只开启当前服务器与活动期间确实存在、并且你愿意自动领取的项目。

## 为什么可以按账号单独配置

不同服务器的活动时间和奖励入口可能不同。AutoMAA 为每个账号生成独立任务文件，因此国服、日服或其他服务器可以使用不同组合。

## 活动界面变化

免费单抽、采矿和限时奖励依赖 MAA 对当前活动界面的支持。活动刚上线时如出现识别失败，可暂时关闭对应选项；不要通过增加大量重试来尝试穿过未知界面。
