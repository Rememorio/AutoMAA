# 常见问题

## macOS 提示“Apple 无法验证 AutoMAA.app”

当前公开版本尚未使用 Apple Developer ID 公证。请先确认安装包来自官方 Release 并校验 SHA-256，然后按照[下载与安装](../guide/installation#处理-apple-无法验证-提示)中的“仍要打开”步骤操作。

不要全局关闭 Gatekeeper，也不要运行来源不明的终端命令来移除系统保护。

## 应用内更新失败

- 确认 AutoMAA 已从 DMG 拖入“应用程序”或其他可写目录，而不是直接在 DMG 中运行；
- 确认能够访问 `api.github.com` 和 GitHub Release 下载地址；
- 再次点击“下载并校验”，不要复用校验失败的安装包；
- 如果提示目录不可写，或重启后仍是旧版本，请从官方 Release 手动下载并覆盖安装；
- 更新失败不会删除用户配置，辅助程序会尽量恢复并重新打开旧版本。

`v0.1.0` 没有应用内更新功能，需要手动安装一次 `v0.1.1`。

## 找不到 `maa-cli`

在终端运行：

```bash
which maa
maa version
```

如果尚未安装：

```bash
brew install MaaAssistantArknights/tap/maa-cli
maa install
```

然后把 `which maa` 输出的路径填入 AutoMAA“全局设置”。Apple Silicon Homebrew 通常为 `/opt/homebrew/bin/maa`。

## 提示 MaaCore 或资源缺失

运行：

```bash
maa install
maa update
```

也可以在 AutoMAA“全局设置”中选择“更新稳定版核心与基础资源”。如果 GitHub 网络不稳定，稍后重试或先确认代理和 DNS；不要删除仍在使用的 MAA 配置目录。

## 提示 MaaCore 与热更新资源不兼容

这表示 MaaCore 无法完整加载基础资源与增量资源组合，不是游戏客户端、账号或 MaaTools 故障。新下载的候选组件会在隔离目录中验证，失败时不会启用；如果当前安装本身已不兼容，AutoMAA 会在启动任何游戏前停止，不会反复尝试客户端，也不会自动回退资源。

- 如果需要立即使用这批最新资源，可在“全局设置 → MAA → 更新核心与基础资源”中选择 Beta，并在确认预发布风险后更新；
- 如果希望只使用稳定版，请等待兼容 Core 进入稳定通道，再执行稳定版更新；
- 空闲自动维护始终使用稳定通道，不会替你切换到 Beta。

## 无法连接 MaaTools

依次确认：

1. 游戏客户端已启动；
2. 当前连接环境已正确安装并启用 MaaTools；
3. 客户端地址与 MaaTools 暴露的地址一致；本机默认通常为 `127.0.0.1:1717`；
4. 没有另一个游戏、MAA 或遗留进程占用端口；
5. 上一个客户端已经完全退出。

查看占用进程可在终端运行：

```bash
lsof -nP -iTCP:1717
```

不要在不清楚进程身份时强制结束它。先关闭相关游戏和 MAA，再重新运行 AutoMAA。

如果占用者是另一个已配置、使用同一地址的客户端，AutoMAA 会在活动记录中直接指出客户端名称并停止流程。即使计划目标客户端也同时运行，只要无法唯一确认端口归属，就不会继续连接或自动关闭任何客户端。

## 多账号匹配失败

- 先确认服务器是否支持：MAA 支持官服、Bilibili、繁中服与 MaaCore v6.16.8 起的韩服自动切换账号；国际服和日服只能使用当前已登录的单个账号；
- 在对应客户端的登录页确认每个账号显示的文本；
- 使用短而唯一的片段，例如末四位；
- 不要填写掩码后所有账号都相同的部分；
- 确认同一客户端的片段没有重复；
- 不要填写密码或完整手机号。

账号匹配失败只会跳过当前账号，其他账号仍可继续。

## 今天的任务被自动跳过

AutoMAA 会记录当天已经成功的任务。如果你确认需要重跑，在“全局设置”中选择“重置今日完成记录”。

这个操作不会恢复游戏中已经消耗的理智、道具或无人机，请先判断是否适合重跑。

## 定时任务没有执行

确认：

- “系统定时任务已安装”；
- 触发时间到达时用户处于登录状态；
- Mac 没有关机或注销，且系统处于唤醒状态；AutoMAA 本身无需保持打开；
- 如果显示器会自动熄灭，已经在电池或节能设置中开启“显示器关闭时防止自动进入睡眠”；
- 没有让 PlayCover 长时间停留在锁屏或登录窗口；
- App 没有被移动、删除或替换到不同路径；
- “活动记录”中是否有对应方案的警告或错误；
- `~/Library/Application Support/AutoMAA/Logs` 中是否有对应时间的 Runner 诊断日志；
- 当时是否已有另一个 AutoMAA 流程持有进程锁。

如果问题发生在刚更新 AutoMAA 之后，请先打开一次新版本并等待侧边栏重新显示“系统定时任务已安装”。新版本会自动刷新已启用的定时任务；不需要先关闭再重新打开每个计划的定时开关。

系统睡眠时无法保证按设定分钟启动，通常会在完全唤醒后补触发；关机期间错过的任务不会补回。详细条件和推荐设置见[每周定时运行](../guide/scheduling)。

## 怎样提交有效的问题报告

在 [GitHub Issues](https://github.com/Rememorio/AutoMAA/issues) 中说明：

- 从“关于 AutoMAA”复制的版本、macOS、芯片及 MAA 环境信息；
- 客户端服务器类型；
- 预期行为、实际行为和复现步骤；
- 已去除敏感信息的相关活动或诊断日志。

不要公开完整手机号、账号片段、密码、验证码、本机用户名或私人路径。
