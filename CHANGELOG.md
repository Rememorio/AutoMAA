# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

### 修复

- MaaCore 即使命令退出码为 0，只要启动输出明确报告 `GameOffline` 或核心初始化失败，AutoMAA 也不会再把账号误报为已就绪；离线恢复仍严格复用每客户端一次的安全重启预算。

## 0.10.0 - 2026-08-30

### 新功能

- 理智作战新增“上次成功的常规关卡”策略：AutoMAA 会按客户端和账号记住 maa-cli 明确报告的最近一次非剿灭成功作战，剿灭、零次作战、失败、超时和取消不会覆盖；缺少记录时会在启动游戏前阻止运行。

### 兼容性

- 配置协议升级为 schema v6，并以完整字段作为唯一基线，不再在解码过程中猜测缺失字段的旧含义。schema v5 及更早配置首次由图形界面读取时会先备份再恢复通用空配置；后台 Runner 只报告不兼容并退出。常规关卡运行状态保存在独立文件中。

### 修复

- MAA 任务不再统一等待固定 15 分钟：公开招募会按招募次数计算上限，基建会区分收菜、完整换班和自定义排班，信用与奖励也使用各自的合理上限；理智作战仍保留长任务预算。

### 可靠性

- 账号准备或任一日常任务超时时，AutoMAA 会安全关闭并重启当前客户端一次，再重新准备账号和重试当前任务；若同一客户端再次超时，则跳过其余步骤并要求人工检查，避免在登录、更新、网络异常或未知弹窗上连续耗尽多个超时。
- 游戏连接离线与命令超时共用每客户端每次流程一次的受控恢复预算；成功步骤断点、客户端串行关闭和端口释放规则保持不变。
- 使用 `--data-directory` 或开发数据目录隔离启动 App 时，自动更新检查、系统定时集成和 MAA 自动维护现在全部禁用，避免隔离验收触碰本机 MAA 安装。

### 内部改进

- 关卡来源解析、常规关卡记忆与对应存储已收敛为独立领域模块；任务文件生成不再维护两套“是否生成”和“如何解析”的分支。
- 图形界面的配置备份恢复由配置存储统一处理，界面入口不再复制不兼容与损坏配置的重置流程；后台 Runner 继续使用严格只读加载路径。

## 0.9.5 - 2026-08-26

### 修复

- 资源兼容性不再依赖 MaaCore 版本号或特定 JSON 字段猜测；AutoMAA 会在独立子进程和隔离用户目录中，让当前 MaaCore 按实际运行顺序加载基础资源、MaaResource、在线热更新以及各服务器与 iOS 差异资源，从而直接识别解析失败、崩溃和超时。
- 核心、基础资源和识别资源更新改为先写入隔离候选目录，完整验证通过后才替换当前组件；下载失败或候选资源与 Core 不兼容时不会污染现有安装，也不会启动游戏。

### 可靠性

- 候选组件的启用采用带恢复的文件替换事务；更新流程仍遵循稳定版自动、Beta 手动确认的通道边界，不会为通过验证而静默升级 Beta 或回退资源。
- 正式 App 新增独立签名的资源验证组件，验证过程不会连接游戏、读取 AutoMAA 账号配置或复用 MaaCore 的真实日志目录。

## 0.9.4 - 2026-08-26

### 修复

- 运行前、手动热更新以及核心维护完成后会检查 MaaCore 与已安装基建热更新资源的兼容性；检测到旧 Core 无法加载新版资源时，会在启动任何游戏客户端前停止并给出明确处理方式，不再让所有客户端重复进入必然失败的账号准备。
- MaaCore 初始化或资源加载失败不再沿用游戏公告、登录弹窗等笼统诊断，也不会继续执行无效的账号准备重试。

### 可靠性

- “更新核心与基础资源”新增需要二次确认的 Beta 入口，用于稳定通道暂未包含兼容 Core 时由用户主动升级；空闲自动维护仍严格使用稳定通道，AutoMAA 不会静默切换预发布版本。
- 资源与 Core 不兼容时不会自动修改或回退 MaaResource 仓库；活动记录会提示选择 Beta 或等待兼容版本进入稳定通道，保留更新决策和资源状态的可追溯性。

## 0.9.3 - 2026-08-25

### 可靠性

- 手动更新 MAA 核心、基础资源或识别资源，以及空闲时的自动维护，遇到可识别的临时网络错误时会在保留首次失败详情后自动重试一次；取消、校验失败等非网络问题仍会立即停止，避免掩盖需要人工处理的异常。
- 自动重试过程与恢复结果会写入活动记录；重试成功不会被计为最终警告，连续失败时仍保留最后一次诊断信息并提示稍后手动重试。

## 0.9.2 - 2026-08-20

### 修复

- 多个客户端共用 MaaTools 地址时，如果另一个已配置客户端仍在运行，AutoMAA 现在会明确指出冲突客户端并提示关闭相关游戏和 MAA 后重试，不再只显示笼统的端口占用错误。

### 可靠性

- 端口已打开且多个已配置客户端同时运行时，AutoMAA 不会仅凭目标客户端进程存在就继续连接；无法唯一确认端口归属时仍会在执行任何游戏任务前安全停止，也不会自动关闭用户手动打开的客户端。

## 0.9.1 - 2026-08-20

### 修复

- 修复打包后的后台 Runner 没有复用 AutoMAA 通知授权、导致定时运行的重要通知被静默跳过的问题；打包与发版验证现在会固定检查 Runner 的通知身份。
- 公招 5★/6★、特殊标签和保留标签现在会在对应公招任务结束后立即通知，不再等待整套方案收尾；流程结束只会重试尚未送达的公招提醒，避免重复通知。

### 可靠性

- 重要通知不再静默失败：后台投递未授权、不可用或失败时会写入活动记录；全局设置新增“测试后台通知”，用于验证定时 Runner 的真实投递链路。
- 打包后的 Release App 现在也会遵守隔离数据目录参数，并拒绝把系统临时目录中的 Runner 注册为正式定时任务，避免界面验收构建覆盖用户的 LaunchAgent。

## 0.9.0 - 2026-08-17

### 新功能

- 自动化总览现在会在每张方案卡上独立显示“已就绪”、提醒或问题摘要；选择状态即可切换下方执行路径和完整运行检查，不必再逐个从侧栏猜测方案是否可运行。
- 总览中的方案状态明确区分“正在查看”和“正在运行”，执行路径与检查标题会直接显示对应方案名称，并提供就近的方案切换入口。

### 可靠性

- 运行检查现在记录问题属于共享配置还是具体方案；其他方案的非阻断提醒不会混入当前方案详情，而会影响统一任务文件生成的结构错误仍会阻止运行并单独说明来源。
- 手动运行、后台定时 Runner 与方案卡按钮继续使用同一套运行前校验，错误配置不会因总览展示方式变化而绕过。

### 兼容性

- 配置协议继续使用 schema v5；现有客户端、账号、方案、定时任务和当日断点无需迁移。

## 0.8.0 - 2026-08-17

### 新功能

- 全局设置新增可选的“空闲时自动更新 MAA 核心与基础资源”。启用后，AutoMAA 在打开且空闲时通过稳定通道更新，每 24 小时最多尝试一次；方案原有的“运行前热更新”继续独立获取可热更新的识别资源。
- 同步 MaaCore v6.16.8 的账号切换能力：韩服现在可以像官服、Bilibili 和繁中服一样配置多个已登录账号，并用唯一 Email 片段匹配；国际服和日服仍只使用当前已登录的单个账号。

### 可靠性

- 自动维护会避让正在运行的手动方案、后台 Runner、AutoMAA 应用更新，以及 90 分钟内即将触发的定时方案；所有 MAA 维护入口共用同一进程锁，避免更新与任务并发修改资源。
- 稳定通道更新会先用 10 秒测速选择镜像，并为完整核心包下载保留最长一小时；失败原因继续写入活动记录和脱敏诊断日志。

### 兼容性

- 配置协议继续使用 schema v5；已有配置可以直接读取，新自动更新选项默认关闭，不会在升级后自行下载 MAA 核心。

## 0.7.5 - 2026-08-13

### 修复

- App 更新后会按 Runner 的版本与构建身份重新注册已启用的定时任务，刷新 macOS 对可执行文件的启动约束；若后台流程正在运行，注册刷新会等待流程结束，避免中断任务。
- MAA 报告 `GameOffline` 时会安全关闭并重启当前客户端一次，再重新准备账号；重启后仍无法连接时才关闭并跳过该客户端。
- 活动记录现在会区分“今天已完成所以跳过”和“因异常未执行”，部分完成的流程会显示完成、失败与未执行步骤数，不再只用 100% 进度和笼统警告表示结束。

## 0.7.4 - 2026-08-12

### 修复

- 账号准备或任务执行在自动重试后成功时，活动记录会保留重试过程并明确标记恢复结果，不再把已经恢复的尝试永久计为警告。
- 自动重试全部耗尽时现在只保留一条可操作警告，并附带对应诊断详情；MaaTools 截图失败会给出更准确的连接异常提示。

## 0.7.3 - 2026-08-10

### 修复

- AutoMAA 在后台定时任务运行期间打开或保持前台时，现在会持续同步活动记录、当前阶段与累计进度，不再停留在启动时读到的旧记录或误报“已准备就绪”。

### 可靠性

- 主 App 现在会识别后台 Runner 持有的运行锁，并在定时任务结束前避让 MAA 配置生成、维护操作和应用更新；后台运行只展示实时状态，不再提供无法作用于该进程的“安全停止”入口。

## 0.7.2 - 2026-08-09

### 改进

- 理智作战成功后，活动记录现在会显示 maa-cli 实际返回的关卡与完成次数，并可展开查看聚合后的总掉落；maa-cli 未返回作战摘要时仍保持原有完成行为。

## 0.7.1 - 2026-08-09

### 修复

- 诊断日志现在同时脱敏命令名称和命令输出，并自动遮盖 macOS 用户目录前缀，避免客户端位于个人目录时记录本机用户名和私人路径。
- 缺少 App Bundle 标识的源码调试可执行文件不再因访问系统通知中心而触发断言退出；正式 App Bundle 的通知行为不变。

## 0.7.0 - 2026-08-08

### 不兼容变化

- schema v5 现在是唯一受支持的配置基线。v0.6.0 生成的完整配置可以直接使用；schema v4 及更早配置不再迁移，图形界面会先备份再恢复通用空配置，后台 Runner 会报告不兼容并退出。
- 当前配置的全局设置、方案周计划和每条周计划规则都必须完整存在；缺失必要字段或仍使用旧版每日时间结构的文件不再由解码器推测和补全。

### 可靠性

- 配置版本检查、损坏配置备份和恢复流程继续保留，避免不兼容数据被静默改写或部分读取。

## 0.6.0 - 2026-08-08

### 新功能

- 每个自动化方案现在可以配置多条“星期 + 时间”规则，例如周一至周六早晨运行、周日晚间运行；侧边栏和总览会显示下一次运行时间。
- 周计划继续使用每个方案独立的 macOS LaunchAgent，同一时刻只在星期与时间都重合时判定为冲突。

### 可靠性

- 同一方案每天最多安排一次，避免第二次触发被当天断点全部跳过；一天内需要运行不同内容时应使用按方案隔离的独立计划。
- 配置协议升级为 schema v5。schema v4 的每日时间会在首次读取时先备份，再自动转换为覆盖周一至周日的单条规则；其他不兼容配置仍按既有策略处理。

## 0.5.1 - 2026-08-06

### 修复

- 修复 `maa-cli` 仅在任务摘要中列出公招结果时，5★/6★ 组合和保留标签未进入活动提醒与系统通知的问题；原有详细输出格式继续兼容。

## 0.5.0 - 2026-08-05

### 新功能

- 全局设置新增“自动下载并准备更新”。启用后，AutoMAA 在打开且空闲时发现正式版本会自动下载，并完成附件大小、SHA-256、磁盘映像、Bundle ID、版本、架构和代码签名校验；安装前仍由用户确认重启。

### 可靠性

- 自动下载会避让正在执行的方案和 MAA 维护操作，流程结束后再继续；后台定时 Runner 不会自行下载或安装 App 更新。
- 已准备的更新可以跨启动恢复，并在重新显示安装入口前再次校验 App；安装阶段会确认没有手动或定时流程占用运行锁，避免替换 App 时与任务并发。
- 旧的 schema v4 配置继续兼容，缺少应用更新偏好时默认关闭，不会触发配置重置。

## 0.4.4 - 2026-08-04

### 改进

- 文档首页与“关于 AutoMAA”新增“直到日常变成一次运行”宣传字图，在不干扰运行状态和安全提示的前提下补充项目视觉表达，并保持深浅色背景下清晰可读。

## 0.4.3 - 2026-08-04

### 修复

- 将页面导航与当前运行方案改为独立状态：进入方案编辑页再返回总览时，会继续保留该方案用于执行路径、运行检查和侧栏运行按钮；侧栏底部的方案菜单现在只切换运行目标，不再意外打开编辑页。

## 0.4.2 - 2026-08-03

### 修复

- 将客户端类型统一写入每个 MAA 任务配置，让繁中服、国际服、日服和韩服的公招、基建、信用商店与领奖任务正确使用对应服务器资源、客户端语义和 PlayCover 适配，不再依赖 Profile 重复指定资源，也不会产生“资源已设置”的误导警告。

## 0.4.1 - 2026-08-03

### 修复

- 优化 PlayCover 客户端的收尾流程：缩短未生效退出请求的等待时间，并在确认客户端进程与 MaaTools 端口均已释放后将系统兜底清理记为正常完成，不再产生误导性的“强制关闭”警告；真正无法关闭或释放端口时仍会安全停止。

## 0.4.0 - 2026-08-03

### 新功能

- 全局设置新增可选的重要通知。用户授权后，手动流程与后台定时 Runner 会通过 macOS 通知中心提醒公招 5★/6★、特殊或保留标签、需要人工处理、流程中止和最终失败的步骤。

### 改进

- 普通成功完成与主动安全停止不发送系统通知，并使用系统默认的普通中断级别，尊重专注模式、定时摘要和用户的声音设置。
- 通知正文只显示结果概括，不包含账号名称、识别标签、路径或具体错误；完整上下文继续保留在活动记录中。
- 通知权限只在用户主动开启功能时请求；全局设置会显示系统授权状态，并在权限关闭时提供通知设置入口。
- 旧的 schema v4 配置继续兼容，缺少通知设置时默认关闭，不会触发配置重置。

## 0.3.4 - 2026-08-03

### 改进

- 将基建“换班心情阈值”明确为“上岗最低心情”，说明它只筛选换班当刻的候选干员，不会在干员工作到阈值时自动再次换班。
- 对低于 90% 的完整换班配置显示非阻断风险提示；已有配置不会被静默改写。

### 修复

- 将新建自定义方案的完整换班上岗最低心情从 MAA 通用默认值 30% 调整为 90%，避免每天只换班一次时选入低心情干员并在下次换班前进入疲劳；MAA 推荐参数模式继续遵循上游 30% 默认值。

## 0.3.3 - 2026-08-03

### 改进

- 明确 AutoMAA 主窗口无需为定时运行保持打开，并补充登录会话、锁屏、显示器熄灭、系统睡眠、合盖与关机状态下的运行条件和推荐电源设置。

### 修复

- 修复 AutoMAA 已打开时不会主动载入后台 Runner 新增活动记录的问题；返回 App、自动化总览或活动记录后会同步最新结果。

## 0.3.2 - 2026-08-03

### 新功能

- 自动识别 maa-cli 的公招结果输出，将 5★/6★ 组合、特殊标签和方案配置的保留标签提升为醒目的活动提醒。
- 公招提醒会带上账号、识别标签和处理建议，并保留在手动与定时运行的活动摘要中；提醒本身不会把已完成的流程误报为失败。

### 改进

- 活动记录的“需留意”筛选涵盖公招提醒、警告与错误；完成摘要不再重复计入警告数量。

## 0.3.1 - 2026-08-02

### 改进

- “关于 AutoMAA”新增反馈与支持入口，可一键复制 App、macOS、芯片、maa-cli、MaaCore 和配置协议版本；复制内容不会包含账号、账号片段、本机路径或运行日志。
- 版本号在关于页改为简洁的版本标记，构建号移入诊断信息；软件更新页只在当前更新状态中展示版本，减少重复信息。
- macOS App 菜单中的“关于 AutoMAA”“设置…”和“检查更新…”会打开主窗口并联动对应页面；帮助菜单新增使用文档和问题反馈入口。
- 主界面收束为单一窗口，避免意外打开多个共享同一运行状态的编辑窗口。
- Bug 报告表单可以直接粘贴应用复制的版本与环境信息。

## 0.3.0 - 2026-08-02

### 改进

- 今日总览按深夜、清晨、上午、中午、下午、晚间和夜间显示不同问候与提示，跨时间段会自动更新。
- 新配置中的轻量日常默认定时调整为 09:00，完整日常调整为 21:00；已有配置不受影响。
- 已启用方案修改时间后会自动同步系统定时任务，侧边栏、总览和方案页只把实际生效的 LaunchAgent 计为定时方案。
- 相同分钟的多个定时方案会在启用前提示冲突，避免同时启动后相互争抢进程锁。
- 将侧栏底部的“工具”分组调整为“应用”，更准确地归纳活动记录、全局设置和关于页面。
- 运行进度条增加百分比，长步骤执行期间也能明确看到已完成的累计进度。
- 活动记录的历史区域改为稳定的被动更新布局，查看既有轮次不会干扰正在执行的流程。
- 当前运行卡新增独立、可滚动的实时活动区，可按需跟随最新事件；当前轮次结束后才会归入历史列表。
- 统一客户端、账号和任务活动文案，账号名称会明确标注为账号，不再出现“正在进入某某”等含义模糊的提示。

### 修复

- 修复连续切换定时开关或快速修改时间时，较早的异步操作可能覆盖新设置的问题。
- 修复 App 更新或移动后仅比较时分、可能把旧 Runner 路径误报为“系统定时任务已安装”的问题。
- 修复启动客户端、切换账号或开始下一任务时会把累计运行进度重置为零的问题。
- 修复活动记录整页跟随最新事件时可能占满主线程，导致界面和客户端切换一同无响应的问题。

## 0.2.2 - 2026-08-02

### 改进

- 客户端、账号和自动化方案的名称编辑改为明确的可编辑控件；侧边栏优先显示自定义客户端名称，并保留服务器类型作为辅助信息。
- 新建同类对象会自动生成不重复的默认名称；重命名继续使用稳定标识，不影响方案引用、当日断点或定时任务。
- 将“运行日志”重构为按每次运行分组的“活动记录”，集中展示当前进度、方案结果、步骤上下文、警告与错误，并支持搜索、筛选、折叠和跟随最新活动。
- 普通活动只显示可执行的用户提示；maa-cli 原始输出改为按需展开的诊断详情，不再挤占主要时间线。
- MAA 核心更新与资源热更新也会进入统一的活动记录，不再成为界面之外的维护状态。

### 可靠性

- 每次运行使用独立标识并保留一份经过脱敏的诊断日志，自动保留最近 30 次；旧版 `history.json` 可继续读取并按日期归入较早记录。

## 0.2.1 - 2026-08-02

### 工程与分发

- 发版门禁新增公开文案检查与打包 App 的隔离启动冒烟测试。

### 改进

- YoStar 服务器改用更常见的“国际服”“日服”“韩服”名称。

### 修复

- 运行前会阻止 MaaCore 尚不支持的 YoStar 英/日/韩服多账号切换，并明确使用游戏当前已登录的单个账号，避免错误传入账号片段后启动失败。
- 方案执行期间的运行按钮会准确显示当前方案、其他方案或维护任务正在运行，不再把忙碌状态误报为“配置未完成”。
- 安全停止可以识别 PlayCover 外层链接 App 对应的实际游戏进程，确保客户端和 MaaTools 端口真正释放后才结束清理。

## 0.2.0 - 2026-08-02

AutoMAA 0.2.0 将早期的单一流程整理为可复用方案，并完成一次不保留实验配置兼容层的协议与交付质量重构。

### 新功能

- 引入可复用的“自动化方案”：客户端与账号只配置一次，每个方案独立选择目标账号、步骤顺序、任务参数和执行策略。
- 首次启动提供“轻量日常”和“完整日常”两个通用模板；支持复制、删除及创建任意额外方案。
- 基建可明确选择 MAA `Infrast mode = 20000` 的仅收菜模式或 `mode = 0` 的完整换班模式。
- 新增 MAA `Mall` 信用与购物任务，支持访友、信用商店、优先购买、黑名单、溢出策略和助战信用关。
- 每个方案可分别手动运行或安装独立的 macOS LaunchAgent 定时任务。
- 基建新增 MAA `mode = 10000` 自定义排班文件与方案序号；`mode = 20000` 改用更准确的“一键轮换 / 仅收菜”说明。
- 理智作战补齐当前 MAA 可导航的长期关卡、固定剿灭和 OF 关卡，并支持关闭、自动及 1–10 次连战。
- 公招新增三星首选标签、额外标签策略和任意保留标签列表。
- 全局设置可以检测 maa-cli / MaaCore 版本，分别更新稳定版核心与基础资源、热更新识别资源。
- 新增“关于 AutoMAA”页面，集中展示版本、定位、本地隐私边界、许可证与 MAA credit。

### 可靠性

- 任务文件与当日断点均加入方案 ID，避免轻量日常和完整日常之间互相跳过步骤。
- 无界面 Runner 必须接收有效的 `--plan` 参数，只执行指定方案。
- 图形界面、定时 Runner 和 `WorkflowRunner` 统一执行结构与运行前校验，重复账号片段、损坏步骤顺序和越界参数不能绕过 UI 直接运行。
- 运行记录统一遮盖配置中的账号片段、完整手机号和邮箱。
- 运行期间编辑配置只保存下一次使用的数据，不会改写当前快照对应的 MAA 任务文件。
- Debug GUI 可以使用完全隔离的数据与 LaunchAgent 目录，并禁止调用真实 `launchctl`。
- 不兼容或损坏的配置会先备份再重置；无界面 Runner 不会静默改写旧配置。

### 改进

- 将全局运行控制移至侧边栏底部，统一展示就绪状态、执行进度与安全停止，避免标题栏按钮遮挡滚动内容。
- 服务器名称使用常见地区称呼：简中服 · 官服、简中服 · Bilibili、繁中服、国际服、日服和韩服。
- 未满足运行条件时，主界面明确显示“配置未完成”，避免禁用的强调按钮看起来仍可点击。

### 修复

- 修复任务步骤关闭后，总开关随参数区域一起被禁用、导致无法重新开启的问题。
- 修复临期理智药仍生成已弃用 `expiring_medicine` 的问题，改用 `medicine_expire_days`。
- 修复公招仍生成已弃用 `skip_robot` 的问题，改用 `preserve_tags`。
- 修复仅替换测试数据目录时仍可能检查真实 LaunchAgent 的隔离漏洞。
- 修复配置 schema 不匹配时静默覆盖原文件的问题。

### 工程与分发

- 新增 macOS 26 CI：运行隔离测试、构建 App、校验签名并在临时目录验证更新器。
- 新增 Issue Forms、Pull Request 模板、Dependabot 与 `SECURITY.md`。
- 新增一键 Release 验证脚本，覆盖 Swift 测试、文档构建、更新器、DMG、SHA-256、挂载结构、版本、架构与签名。
- MAA 参数与服务端映射已同步至 MaaAssistantArknights / MaaCore v6.16.1 的集成协议。
- 配置协议升级为 schema v4。v0.1.x / schema v2 配置不迁移；首次打开 0.2.0 时会备份旧文件并恢复通用空配置。

## 0.1.1 - 2026-08-02

### 新功能

- 启动时自动检查 AutoMAA 的最新正式 GitHub Release，也可从 App 菜单或“全局设置”手动检查。
- 支持在 App 内下载并校验更新，准备完成后可“重启并立即更新”。

### 安全与可靠性

- 更新器只接受本项目固定命名的 Apple Silicon DMG 与对应 SHA-256 文件。
- 安装前依次校验附件大小、SHA-256、磁盘映像、Bundle ID、版本、arm64 架构与 macOS 代码签名。
- 使用独立辅助进程替换 App；新版本校验或重新启动失败时自动恢复旧版本，并在下次启动显示结果。
- 从只读 DMG 或不可写目录运行时不会尝试覆盖 App，而是提示用户手动安装。

### 分发

- 新增可重复执行的更新器替换冒烟测试。
- `v0.1.0` 本身尚无应用内更新能力，需要手动安装一次 `v0.1.1`；之后即可使用应用内更新。

## 0.1.0 - 2026-08-01

AutoMAA 的首个公开版本。

### 新功能

- 提供原生 macOS SwiftUI 图形界面和无界面定时运行器。
- 支持动态配置多个客户端、多个账号及其执行顺序。
- 支持简中服 · 官服、简中服 · Bilibili、繁中服、国际服、日服和韩服资源。
- 支持理智作战、公开招募、不换班基建收菜与奖励领取。
- 每个任务可在 MAA 推荐参数和 AutoMAA 自定义参数之间切换。
- 支持步骤重试、当日断点、客户端安全关闭、连接释放确认、单实例锁和防休眠。
- 支持 macOS LaunchAgent 每日自动运行和 MAA 资源热更新。
- 运行时工具栏使用阶段状态胶囊和克制的呼吸提示，可随时安全停止且不会因状态文字变化跳动。

### 可靠性

- 对账号匹配失败、强制游戏更新、数据包下载、维护、网络异常和客户端缺失提供分级处理与人工提示。
- 强制游戏更新或数据下载未完成时不会尝试绕过未知界面，也不会阻塞后续可安全执行的客户端。

### 分发

- 提供 Apple Silicon DMG 安装包和 SHA-256 校验文件。
- 当前构建使用临时代码签名，尚未进行 Apple Developer ID 签名与公证。
