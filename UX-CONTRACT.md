# AutoMAA 界面行为约定

视觉规则见 [DESIGN.md](DESIGN.md)。界面使用简体中文及原生 macOS 控件；配置与运行约束由 `AutoMAAKit` 校验，界面不另建一套参数语义。

| 能力 | 统一入口与行为 |
| --- | --- |
| 页面与滚动 | `AppPage` 管理内容列和页面滚动；活动记录保留固定筛选栏，实时记录仅在自己的区域滚动 |
| 导航 | `NavigationSplitView` 与侧栏选择管理页面；客户端、账号和方案均有可用的键盘选择入口 |
| 输入与选择 | 使用 SwiftUI `TextField`、`Picker`、`Toggle`、`DatePicker`；系统负责输入法、焦点、弹出菜单、日历和滚动条 |
| 名称编辑 | `EditableDisplayNameField` 管理编辑状态；结束编辑时规范化显示名称，配置修改自动保存 |
| 开关 | 设置项由 `SettingsToggleRow` 展示名称、用途和右侧开关；任务参数卡保持独立的任务启用与自定义参数开关 |
| 顺序 | `ReorderButtons` 提供带对象名称的上移、下移按钮；首尾禁用，无需拖动也能完成排序 |
| 提示 | `AppModel.showBanner` 与 `RootView` 提供可关闭的结果提示，覆盖层不推动页面；重要错误保留在所属设置或活动记录中 |
| 更新 | 自动和手动更新共用 `UpdateProgressRow`、`UpdatePolicy` 及同一生命周期；取消后保持忙碌直至清理结束。发布说明独立于执行状态保留，下载与失败不会隐藏内容入口 |
| 更新内容 | `UpdateDetailsView` 供设置、关于和活动记录共用；原生滚动窗口显示版本、结构化正文与来源。读取失败保留已有内容，可单独重试；关闭详情只取消说明请求 |
| 停止 | 工作流称“安全停止”，独立维护称“取消更新”；`StopOperationButton` 在侧栏与活动页保持同样的状态与行为 |
| 详情 | `DetailDisclosure` 使用系统展开控件；长内容自动换行，支持选择和复制，不要求横向拖动才能阅读 |
| 删除与重置 | 使用原生确认对话框，说明对象及后果；重置今日完成记录说明成功步骤将重新执行，危险操作与保存分开 |

活动记录搜索方案、客户端、账号、任务名称、消息和详情；首尾空白忽略，清除搜索立即恢复结果并将焦点返回输入框。“需留意”与搜索同时生效。筛选保留在当前应用会话的内存中，退出后清空，不把可能包含账号的搜索词写入磁盘。

历史记录由现有存储上限约束，列表按需创建视图；展开状态属于页面。空历史与筛选无结果分别说明，后者提供清除筛选操作。当前运行始终保留状态和停止入口，筛选只影响显示的记录，不改变运行行为。

AutoMAA 本版本说明随应用提供；新版本完整说明在发现时缓存，跨版本历史按需获取。更新成功的未读提示保留到用户打开当前版本说明。“关于”始终提供回看入口。MAA 的版本与资源修订由更新流程记录，只有启用成功后才出现更新后的安装信息；上游说明须匹配来源和实际版本。缺少信息时明确标注，不能将其他 GUI 的变化或最新版本说明当成本次实际变化。

成功、警告和错误均有文字或符号，不仅依赖颜色。名称较长或窗口变窄时优先换行，路径截断时仍可复制并查看完整值。系统负责控件焦点、按键与菜单行为；自定义动画遵循“减少动态效果”。
