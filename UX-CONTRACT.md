# AutoMAA 界面行为约定

视觉规则见 [DESIGN.md](DESIGN.md)。界面使用简体中文及原生 macOS 控件；配置与运行约束由 `AutoMAAKit` 校验，界面不另建一套参数语义。

| 能力 | 统一入口与行为 |
| --- | --- |
| 页面与滚动 | `AppPage` 管理内容列和页面滚动；活动记录的筛选栏位于当前待办之后，滚到记录区时吸顶，实时记录仅在自己的区域滚动 |
| 导航 | `NavigationSplitView` 与侧栏选择管理页面；客户端、账号和方案均有可用的键盘选择入口 |
| 输入与选择 | 使用 SwiftUI `TextField`、`Picker`、`Toggle`、`DatePicker`；系统负责输入法、焦点、弹出菜单、日历和滚动条 |
| 名称编辑 | `EditableDisplayNameField` 管理编辑状态；结束编辑时规范化显示名称，配置修改自动保存 |
| 开关 | 设置项由 `SettingsToggleRow` 展示名称、用途和右侧开关；任务参数卡保持独立的任务启用与自定义参数开关 |
| 顺序 | `ReorderButtons` 提供带对象名称的上移、下移按钮；首尾禁用，无需拖动也能完成排序 |
| 提示 | `AppModel.showBanner` 与 `RootView` 提供可关闭的结果提示，覆盖层不推动页面；重要错误保留在所属设置或活动记录中 |
| 更新 | 自动和手动更新共用 `UpdateProgressRow`、`UpdatePolicy` 及同一生命周期；取消后保持忙碌直至清理结束。发布说明独立于执行状态保留，下载与失败不会隐藏内容入口 |
| 更新内容 | `UpdateDetailsView` 供设置、关于和活动记录共用；原生滚动窗口显示版本、结构化正文与来源。读取失败保留已有内容，可单独重试；关闭详情只取消说明请求 |
| 停止 | 工作流称“安全停止”，独立维护称“取消更新”；`StopOperationButton` 在侧栏与活动页保持同样的状态与行为 |
| 运行与续跑 | `PlanRunButton`、侧栏及菜单共用 `AppModel.runTitle` 与 `AutoMAAKit.PlanContinuation`；有待确认作战和其他可执行任务时显示“继续其他任务”，仅有可补跑任务时显示“继续未完成”，只剩待确认时显示“结果待确认”，全部处理完毕后禁用 |
| 每周剿灭 | `WeeklyAnnihilation.swift` 决定各账号的游戏周状态，方案页使用原生开关与星期选择；独立于 MAA 参数模式。参与方案共享已满和待确认状态，每日常规断点保持独立 |
| 作战结果处理 | `PlanContinuation.fightRecoveryItems` 从当前日常与周状态生成处理项，不依赖历史日志或筛选。活动记录顶部“需要处理”与方案设置共用 `FightRecoveryActions`；确认本周已满仅保存周记录，不启动游戏。运行中入口保留、禁用并解释原因；重新尝试作战明确说明目标、资源消耗和单项范围。判定依据见[作战规则](docs/tasks/fight.md) |
| 详情 | `DetailDisclosure` 使用系统展开控件；长内容自动换行，支持选择和复制，不要求横向拖动才能阅读 |
| 删除与重置 | 使用原生确认对话框，说明对象及后果；重置今日完成记录说明成功步骤将重新执行，危险操作与保存分开 |

`ActivitySearchField` 搜索历史运行中的方案、客户端、账号、任务名称、消息和详情；首尾空白忽略，清除搜索立即恢复结果并将焦点返回输入框。“全部／提醒／失败”使用原生分段选择，与搜索同时生效，保留整次运行上下文。提醒包含警告、错误、未完成与待补剿灭；失败依据最终结果或失败步骤，不把已恢复的错误当作最终失败。记录区持续显示结果数量与范围，有筛选条件时提供“清除筛选”。条件保留在当前应用会话的内存中，退出后清空，不把可能包含账号的搜索词写入磁盘。

`PlanContinuation.pendingItems` 按方案、客户端、账号和任务断点生成可续跑项；作战阶段沿用调度器的阶段判断。`PendingWorkList` 在总览和活动记录共用按账号折叠的范围与原因展示，按钮文案不随具体任务增加特例。今日未开始运行时只展示简短状态与有效定时的下一次时间，不展开未来任务；待确认处理入口保持独立。失败与待确认作战继续使用既有恢复入口。历史摘要保存当次待补打数量，当前待办从最新状态推导。

活动记录先展示跨方案的“需要处理”，按方案折叠待核实项；随后展示当前运行或所选方案今日状态。历史按开始日期分组，单次运行使用紧凑行，默认折叠且不因刷新自动展开。展开后先展示账号任务结果，再按需查看提醒和完整过程；过程内可单独只看警告与错误，不影响原摘要。历史不承载当前续跑按钮，当前状态不受搜索、筛选或清除历史影响。历史由现有存储上限约束，按日期按需创建视图；展开状态属于页面。空历史与筛选无结果分别说明，后者提供清除筛选操作。当前运行始终保留状态、停止入口和完整实时记录，跟随最新只滚动当前运行卡。

AutoMAA 本版本说明随应用提供；新版本完整说明在发现时缓存，跨版本历史按需获取。更新成功的未读提示保留到用户打开当前版本说明。“关于”始终提供回看入口。MAA 的版本与资源修订由更新流程记录，只有启用成功后才出现更新后的安装信息；上游说明须匹配来源和实际版本。缺少信息时明确标注，不能将其他 GUI 的变化或最新版本说明当成本次实际变化。

成功、警告和错误均有文字或符号，不仅依赖颜色。名称较长或窗口变窄时优先换行，路径截断时仍可复制并查看完整值。系统负责控件焦点、按键与菜单行为；自定义动画遵循“减少动态效果”。

关卡策略说明与账号目标行遵循[理智作战规则](docs/tasks/fight.md)：区分跟随游戏的新常规关卡、用于恢复的记录和临时兜底。缺少记录时说明运行时识别与手动设置入口；待确认续跑明确显示已派发目标。策略仍使用原生 Picker，目标编辑复用原生表单，不新增确认步骤。
